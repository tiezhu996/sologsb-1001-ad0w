# sologsb-1001 · 多语言字幕与配音脚本对齐工作台

> 端口：宿主 `10001` → 容器 `80`

## 原始需求

我想从0做一个多语言字幕与配音脚本对齐工作台，技术栈用Vue3+Vite+TypeScript+Pinia+Element Plus。用户可以导入SRT和剧本文本，按时间码切分、合并或移动台词，给每条台词绑定角色、语速和术语，并在修改后提示前后语气或称呼是否冲突。需要支持键盘逐条校对、撤销重做、时间轴缩放、角色筛选、版本快照和SRT导出，切换界面语言后菜单、快捷键提示和错误信息都要同步变化。先只做前端，数据存IndexedDB，断网和刷新后继续编辑，同一浏览器多标签页同时打开时不能互相覆盖。
